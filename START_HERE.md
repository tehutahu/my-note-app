# 実装引き継ぎ書

この文書を指定して「これを見て行ってください」と依頼されたエージェントは、以下の計画を実行する。設計を説明し直して終了せず、未完了の最初の工程から進める。

## 1. 作るもの

Galaxy Tab S7+とGalaxy S23 UltraでS Penを使う、日本語UIの手書きノートPWA。WindowsのChrome/Edgeでも閲覧・編集する。ペン・蛍光ペン・筆跡消しゴム・図形・太さ・色・背景・複数ページ・フォルダ階層・PDF取り込み/出力・バックアップを備える。

初期版は端末内保存＋専用ファイルの手動受け渡し。自動同期、ユーザーアカウント、課金、OCR、共同編集、Samsung Notes独自形式の直接読み込みは対象外。PDF上の元の文字は編集せず注釈を重ねる。

## 2. 読む順序と判断の優先順位

1. `AGENTS.md`：作業規約。
2. `docs/progress.md`：現在の実装状況、承認、環境の障害。
3. `docs/architecture.md`：採用構成、データ形式、描画・保存・配布仕様。
4. `docs/acceptance.md`：計測手順を含む定量的な完了条件。
5. `docs/feature-review.md`：ユーザーへの機能レビューの出し方。
6. `docs/design.md`：背景と当初の構成比較。

最新のユーザー指示を最優先にする。新しい実行仕様は本書・詳細技術設計・受入基準であり、初期設計の未決定事項はこれらで具体化している。仕様を変えるときは理由と関連する受入IDを記録し、勝手に合格条件を下げない。

## 3. 最初の作業

1. 作業ディレクトリ、適用されるAGENTS、既存ファイル、git状態とremoteを確認する。既存編集を保持する。
2. `docker version` と `docker compose version` でCLIだけでなくEngine接続も確認する。
3. 動かなければホストにNode等を入れて代替しない。文書・ソース・設定の作成は進め、テストは未実行と記録する。Docker DesktopのWSL連携等、ユーザー操作が必要な具体的な一点を伝える。
4. 本番依存の承認記録を確認する。未承認ならPDF.js (`pdfjs-dist`) と `pdf-lib` の2件について用途をまとめて一度だけ確認し、返答待ちでも依存しない工程を進める。
5. M0から実装する。バージョンはコンテナ内で互換性を確認して厳密に固定し、推測した最新バージョンを記入しない。
6. 初期状態には有効なgitリポジトリがなかった。`git rev-parse` で再確認し、利用可能なら初期化する。アクセス制限のある `.git` を削除・変更して回避しない。gitが使えなくてもファイル作業は継続できる。

## 4. 工程と依存関係

| 工程 | 成果物 | 主な受入ID | 依存 |
| --- | --- | --- | --- |
| M0 環境 | Dockerfile、compose.yaml、固定lockfile、テスト起動、CI、最小画面 | ENV-01〜04 | Docker実行可能 |
| M1 ペン試作 | 筆圧ペン、消しゴム、Undo/Redo、指移動・ズーム、入力計測 | INK-01〜06、PERF-01 | M0 |
| M2 保存・整理 | ノート/ページ/フォルダ、図形、背景、保存状態、ゴミ箱 | NOTE-01〜05、DATA-01〜04 | M1自動検証 |
| M3 受け渡し | 専用形式、全体バックアップ、コピー取り込み | XFER-01〜04 | M2 |
| M4 PDF | ページ取り込み、注釈、全ページ出力、エラー処理 | PDF-01〜05 | M2、本番依存承認 |
| M5 PWA・統合 | HTTPSレビュー版、オフライン、更新、レスポンシブ、全項目再現 | PWA-01〜04、UI-01〜03、PERF-02〜04 | M3、M4 |

M1を早期にHTTPSで確認できるようにする。M5まで実機レビューを遅らせない。M1の実機確認待ちでもM2のデータ層等は進めてよいが、PWAの実用性を確定扱いしない。実機で不適合が出たら入力改善を優先し、解消できなければ測定結果を添えて構成変更を相談する。

各工程は「工程全体を最後に見せる」のではなく、確認できる機能ごとにレビュー票を作る。返答がない場合は「ユーザー確認待ち」のまま独立した実装を継続する。ユーザーのコードレビューやPR承認を通常の作業待ち条件にしない。

## 5. TDDの実行規約

各振る舞いについて次を順番に実行する。

1. 受入IDと期待する観測結果を決める。
2. 最小のテストを追加し、対象テストを実行して失敗を確認する。起動設定や構文エラーではなく、未実装の振る舞いが原因であることを確認する。
3. その失敗を直す最小実装を加える。
4. 対象テストを成功させ、周辺をリファクタリングする。
5. JavaScript/TypeScript変更後は `docker compose run --rm app npm test` を実行する。機能完成時には `check` と関連E2Eを実行する。
6. レビュー票にRED時のコマンド・期待不一致・終了コード、GREEN時の結果、テスト名、commitを記録する。ログは `artifacts/` に保存する。テストを後から書いた場合は先行したと偽らず、是正内容を記録する。

新規プロジェクトのテストランナー導入自体はブートストラップとして扱う。その後の機能・不具合修正はテスト先行。設定・文書にも必要な検証を行うが、TDDの記録件数を増やすための無意味なテストは作らない。

## 6. 実装するコマンドの契約

以下はM0で提供する予定のインターフェース。現時点では未実装であり、成功確認済みのコマンドではない。

| コマンド | 動作 |
| --- | --- |
| `docker compose build` | 固定バージョンで開発/テスト/プレビューのイメージを構築 |
| `docker compose run --rm app pnpm install --frozen-lockfile` | プロジェクト専用volume内に依存を再現 |
| `docker compose run --rm app npm test` | Vitestによるunit＋integration。watchなし、失敗時非0 |
| `docker compose run --rm app pnpm run check` | lint、型検査、カバレッジ付きテスト、production build |
| `docker compose run --rm e2e` | production buildを配信してPlaywrightを実行。ブラウザーもコンテナ内 |
| `docker compose up app` | localhost:5173で開発版。Service Workerは無効 |
| `docker compose up preview` | localhost:4173でproduction版。PWA/オフライン検証用 |
| `docker compose down` | 本プロジェクトのサービス停止。依存volumeは保持 |

E2Eサービスは必要なビルドとHTTP起動・ready待ちを自動化し、上記コマンド単独で動くようにする。手作業で別のサーバーを立てる前提にしない。

## 7. GitHubと機能単位の提供

ghと公開リポジトリの利用はユーザーが許可済み。今セッションでは作成しないが、実装エージェントは認証・remoteを確認して利用してよい。既存remoteがあれば再利用。なければ認証済み個人アカウントの `my-note-app` を候補にし、同名があれば内容を調べて無関係なリポジトリに書き込まない。名前が衝突する場合は `my-note-app-pwa` 等の未使用名を選び、記録する。

機能ごとにbranchとPRを用意し、受入ID、ユーザー向け確認方法、自動チェック結果を記載する。CI合格・差分のセルフレビュー後は、ルールが許せば自分でマージしてよい。ブランチ保護を弱めたりテストを迂回したりしない。機能の実機合格はPRのマージと別に扱う。

公開物にはソース、合成サンプル、匿名のテスト結果だけを含める。アプリ内でユーザーが読み込んだPDF・ノートはGitHubへアップロードしない。gh認証情報をコンテナイメージに含めない。

レビュー用の静的HTTPS配信はGitHub Pagesを第一候補とする。課金・独自ドメイン・同期サーバーは不要。公開サイトを作る際は実際の権限と適用される承認条件に従い、必要ならビルドと公開対象を完成させてから確認する。公開できない間もローカルレビュー版と成果物は用意する。

## 8. 完了・引き継ぎ

- エージェントの実装完了：`docs/acceptance.md` の自動項目すべて合格、CI/ビルド成功、全機能にレビュー票、未解決のデータ消失・起動不能なし。
- 日常利用版の完了：上記に加えTab S7+、S23 Ultra、Windowsの指定実機レビューに合格。自動テストだけでこの状態にしない。
- 各セッション終了時：`docs/progress.md` に実際の状態、テスト結果と証拠、未回答の確認、次の一手を保存する。最終回答では実装した機能・確認URL/手順・未検証点を説明する。
- 数値目標は予定値であり現在の達成値ではない。未達なら実測値と改善案を示す。
