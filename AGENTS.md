# このプロジェクトの作業規約

- 最初に `START_HERE.md` と、そこで指定された文書を読む。
- TDDで進める。振る舞いの失敗テスト→失敗理由確認→最小実装→成功→リファクタリングの証拠を機能単位で残す。
- Node、pnpm、npm、テスト、ビルド、Playwrightはコンテナ内でのみ実行する。ホストへのパッケージ・ブラウザー・ランタイムのインストールは禁止。
- JavaScript/TypeScript変更後はコンテナ内の `npm test` を必ず実行する。依存導入にはpnpmを使う。
- 新しい本番用依存関係は導入前に確認する。PDF.js/pdf-libは設計候補であり、まだ導入承認を得ていない。一度得た承認は進捗に記録し、再確認しない。
- ホストでは既存のgit/gh、Docker操作、文書・ソース編集を行ってよい。システム設定変更は自動で行わない。
- ユーザーはコードレビューをしない。機能単位に動作URL、操作手順、期待結果、証拠、制限を提示する。
- 自動検証合格とユーザーの実機確認を別々に記録する。未実行・未回答を合格としない。
- 公開リポジトリとghの利用は許可されている。個人ノート、認証情報、ローカルバックアップをコミットしない。
- セッション終了時は `docs/progress.md` を更新する。未完了の受入ID、次のコマンド、環境上の障害を具体的に残す。
