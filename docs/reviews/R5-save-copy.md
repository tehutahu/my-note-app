# R5 保存開始の遅延を抑える

- 状態：性能目標合格、保存回帰検証中・公開前
- 対象：PERF-03、DATA-02/03
- 操作：通常どおり筆記し「保存済み」を確認します。ノートの形式や内容は変えません。

固定版436f410の再測定で保存開始最大322.40msとなり、300msを超えました（PERF-fixed-labeled.log）。汎用のstructuredCloneに大量の筆跡点を渡す処理を、ノートschema上の可変フィールドを直接コピーする処理に変更しました。背景、点、図形、PDF viewBox、ページID配列を独立させる回帰テストを追加しています。性能の失敗が先行し、コピー独立性は既存契約の回帰補強です。

PERF-copy-check.log：npm test57/check成功。PERF-copy-targets.log：4CPU/8GiBでS/L/P3成功（1.1分）。Sは1020イベント・10保存で入力p95 0.30ms、保存開始中央値17.55ms/最大36.80ms、commit中央値293.60ms、失敗0。PWA hash bc344177376f94cf（未コミット測定）。固定ソースでも再確認します。

Androidの性能Hは未実施です。通常筆記の実機肯定結果を、この性能数値の証拠には用いません。

PERF-copy-browser.log：Chromiumの保存/競合/診断5成功（16.5秒）、Firefox保存/診断4成功（32.0秒）、retry0。200筆の20回再読み込み比較も含みます。
